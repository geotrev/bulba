// import * as utils from "../utilities"

describe("createUUID", () => {
  it("generates a valid UUID of 18-19 characters", () => {})
})

describe("forEach", () => {
  it("iterates over an array", () => {})
  it("breaks the loop if an iteration returns false", () => {})
})

describe("toKebabCase", () => {
  it("converts camelCase", () => {})
  it("converts PascalCase", () => {})
})

describe("type checking", () => {
  describe("getTypeTag", () => {
    it("returns 'function' for functions", () => {})
    it("returns 'object' for objects", () => {})
    it("returns 'string' for strings", () => {})
    it("returns 'undefined' for undefined", () => {})
    it("returns 'null' for null", () => {})
    it("returns 'boolean' for booleans", () => {})
    it("returns 'symbol' for symbols", () => {})
  })

  it("isPlainObject: returns true if value is an object", () => {})
  it("isPlainObject: returns false if value is anything else", () => {})

  it("isFunction: returns true if the value is a function", () => {})
  it("isFunction: returns false if the value is anything else", () => {})

  it("isString: returns true if the value is a string", () => {})
  it("isString: returns false if the value is anything else", () => {})

  it("isUndefined: returns true if the value is undefined", () => {})
  it("isUndefined: returns false if the value is anything else", () => {})

  it("isEmptyObject: returns true if the object is empty", () => {})
  it("isEmptyObject: returns false if the object has entries", () => {})
  it("isEmptyObject: returns false if the value isn't an object", () => {})
})
